# Daymate 배포 후 스모크 테스트 (7스텝)

Railway 배포 URL을 받으면 아래 순서로 **5분 내** 검증한다. 실패 시 Railway 로그 +
환경변수(`SECRET_KEY`/`DATABASE_URL`) 주입 여부를 1차로 의심한다.

| # | 절차 | 합격 기준 |
|---|------|-----------|
| 1 | `[URL]/` 접속 | 가입/로그인 폼 노출 |
| 2 | 신규 이메일·비번 가입 | 첫 세팅(10문항 위저드) 진입 |
| 3 | 세팅 저장 | 오늘 탭에 자동 일정(타임테이블) 생성 |
| 4 | 체크박스 1개 클릭 | 달성률 % 즉시 갱신 |
| 5 | 로그아웃 → 재로그인 | 데이터 유지(유저 격리) |
| 6 | 도구탭 👤계정 → 계정 삭제(비번) | 데이터 소거·로그아웃 |
| 7 | `/privacy.html` 접속 | 200 + CONTACT_EMAIL이 실제 운영자 이메일로 교체됨 |

## 실패 시 1차 점검
- **502/크래시**: 빌드 로그에서 gunicorn 기동 실패 여부.
- **세션 안 풀림/로그인 직후 튕김**: `SECRET_KEY` 미설정 또는 매 배포마다 변동.
- **데이터 재배포 후 소실**: `DATABASE_URL` 미설정 → SQLite 휘발. Postgres 연결 확인.
- **privacy 문의 안내가 placeholder**: CONTACT_EMAIL 미교체(공개 전 단일 블로커).
